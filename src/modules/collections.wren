foreign class Grid {

    construct new(width, height) {}

    foreign width
    foreign height
    foreign fill(value)
    foreign row(x, y, count, value)
    foreign peek(x, y)
    foreign poke(x, y, value)

}
