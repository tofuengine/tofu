import "lib/other" for Other

class Main {

    static initialize() {
        System.write("Main.initialize()")
    }

    static update(dt) {
    }

    static render() {
    }

    static terminate() {
        System.write("Main.terminate()")
    }

}